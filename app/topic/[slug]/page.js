import { notFound } from 'next/navigation';
import ArticleCard from '../../../components/ArticleCard';
import { articles, getTopic } from '../../../lib/content';
export default async function TopicPage({params}){const {slug}=await params; const topic=getTopic(slug); if(!topic)notFound(); const items=articles.filter(a=>a.category.toLowerCase()===slug); return <main className="topic-page"><section className="topic-hero"><div className="eyebrow">TOPIC HUB</div><h1>{topic.name}</h1><p>{topic.dek}</p></section><section className="topic-content"><div className="section-heading"><div><div className="section-kicker"><span>01</span> START HERE</div><h2>Understand the field.</h2></div></div>{items.length?<div className="cards-grid">{items.map(a=><ArticleCard key={a.slug} article={a}/>)}</div>:<div className="empty-state"><h3>This hub is ready for expansion.</h3><p>The V1 architecture includes this topic so OpenHands can add the planned launch library without changing navigation or URLs.</p></div>}</section></main>}
